'use client'

import styled from "styled-components";

export const BG = styled.div`
    position: fixed;
    height: 100%;
    width: 100%;
    opacity: 0.95;
    background-color: rgb(18, 18, 18);
    z-index: -1;
`